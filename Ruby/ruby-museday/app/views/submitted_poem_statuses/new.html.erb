<% stylesheet_link_tag "global" %>

<h1>New Submitted Poem Status</h1>

<%= form_for :submitted_poem_status, :url => submitted_poem_statuses_path do |f| %>
  <% if @submitted_poem_status.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@submitted_poem_status.errors.count, "error") %> prohibited
      this submitted poem status from being saved:</h2>
    <ul>
    <% @submitted_poem_status.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
   <p>
     <%= f.label :name %><br />
     <%= f.text_field :name, :id => "title_field" %>
   </p>

   <p>
   <%= f.submit %>
   </p>
<% end %>

<%= link_to 'Back', submitted_poem_statuses_path %>
